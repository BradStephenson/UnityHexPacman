using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyNewScript : MonoBehaviour {

    private float growRateVert, growRateHoriz;
	// Use this for initialization
	void Start () {
        Debug.Log("Hello World");

       // growRateHoriz = 0.001F;

       // growRateVert = Random.

	}
	
	// Update is called once per frame
	void Update () {
      //  transform
       // transform.localScale += new Vector3(0.001F, 0.002F, 0.00F);
    }
}
